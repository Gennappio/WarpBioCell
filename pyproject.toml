[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "warpbiocell"
version = "0.0.1"
description = "GPU-accelerated agent-based biological tissue simulator built on NVIDIA Warp"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "warp-lang>=1.17",
    "numpy>=1.26",
    "pyyaml>=6",      # experiment configurations are YAML (AGENTS.md "Configuration and units")
]

[project.optional-dependencies]
plots = [
    "matplotlib>=3.8",  # run figures; the runner skips them when it is not installed
]
dev = [
    "pytest>=8",
    "matplotlib>=3.8",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "gpu: requires a CUDA device (skipped automatically when none is present)",
]
